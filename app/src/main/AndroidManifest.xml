<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scame.testingsamples">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".samples.HandlerSample">

        </activity>

        <activity android:name=".samples.HttpEnqueueSample">

        </activity>

        <activity android:name=".samples.HttpRxSample" />

        <activity android:name=".samples.HttpSynchronousSample">

        </activity>

        <activity android:name=".samples.HttpEventBusSample" />

        <activity android:name=".samples.ServiceLongActionSample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".FileDownloadService" />
    </application>

</manifest>